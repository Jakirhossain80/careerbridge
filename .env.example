# CareerBridge uses per-application environment files.
# Copy client/.env.example to client/.env.local and server/.env.example to server/.env.
# Never place credentials in this root file or commit local environment files.
